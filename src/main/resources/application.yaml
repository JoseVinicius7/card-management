spring:
  name: ms-card-management
  datasource:
    url: jdbc:postgresql://localhost:5432/cartoesdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false
